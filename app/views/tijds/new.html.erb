<h1>Items#new</h1>

<%= form_for @tijd, url: {action:"create"}, multipart: true do |f| %>

	<%= label_tag(:machines, "Machine:") %><br />
	<%= f.collection_select :machines, Machine.all, :machine_id, :name %>

	<br /><br />

	<%= f.submit "Reserveren" %>

<% end %>