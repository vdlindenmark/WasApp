<h1>Items#new</h1>

<%= form_for @tijd, url: {action:"create"}, multipart: true do |f| %>

	<%= label_tag(:machines, "Machine:") %><br />
	<%= f.collection_select :machines, Machine.all, :kind, :machine_id  %>
	
	<br /><br />

	<%= f.submit "Reserveren" %>

<% end %>